import { MarginLedger } from 'components/margins/margin-ledger';

export const metadata = {
  title: 'Produce Margin Ledger'
};

export default function AdminMarginsPage() {
  return <MarginLedger />;
}
